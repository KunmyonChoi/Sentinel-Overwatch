---
format: 1920x1080
duration: 14m10s
message: "보안을 몰라도, 내 컴퓨터에 무슨 일이 있었는지 알고 스스로 조치할 수 있다"
arc: "왜 필요한가 → 화면 구성 → 시나리오 셋 → 할 수 있는 일과 없는 일 → 설치 → 설정 → 아키텍처 다섯 장 → 맺음말"
audience: "개인 컴퓨터나 작은 서버를 혼자 관리하는 비전문가, 그리고 이 도구를 검토하는 사람"
mode: collaborative
---

# 계획

## 시청자의 흐름

문제 인식(왜 필요한가) → 지도 그리기(화면 구성) → 손으로 만져보기(시나리오 셋) →
경계 확인(할 수 있는 일과 없는 일) → 내 서버에 올리기(설치·설정) →
믿어도 되는지 확인(아키텍처) → 정리(맺음말).

시나리오 셋을 한가운데 두는 이유: 이 도구의 주장은 "쉽다"이고, 주장은 보여줘야
믿긴다. 앞의 두 장은 시나리오를 이해할 준비이고, 뒤의 네 장은 시나리오를 본
사람이 자연스럽게 갖는 질문(진짜 되나 · 어떻게 깔지 · 믿어도 되나)의 순서다.

## 리듬과 길이를 정하는 것

**내레이션 자막이 길이를 정한다.** 음성이 없으므로 자막이 곧 내레이션이고,
읽는 속도(한국어 초당 약 5.3자 + 문단 사이 호흡)가 각 장면의 길이가 된다.
자막은 화면 아래 고정 띠에 항상 보이며, 문단 단위로 교체된다.
따라서 각 장면의 시각 요소는 **자막 문단이 바뀔 때마다 하나씩** 도착한다.

## 시각 언어

제품이 쓰는 두 팔레트를 그대로 쓴다. 영상이 제품과 다른 얼굴이면 안 된다.

- **초보자 화면 / 해설 장면** — 바탕 `#eef3f0`, 카드 `#ffffff`, 글 `#17221d`,
  흐린 글 `#5c6b64`, 강조 `#0e7c6b`, 선 `#dfe7e3`. 서체 IBM Plex Sans KR.
- **전문가 화면 / 터미널 장면** — 바탕 `#0a0f0d`, 형광 초록 `#00ff41`,
  경고 빨강 `#ff0033`. 고정폭 서체.
- **자막 띠** — 화면 아래 220px. 초보자 팔레트에서는 `#17221d` 바탕에 흰 글씨,
  전문가 팔레트에서는 그대로 이어져 장면이 바뀌어도 자막의 자리는 흔들리지 않는다.

## 모션의 절제 — 의도한 것

블루프린트는 제품 광고에서 뽑힌 형태라 기본값이 화려하다. 이 영상은 광고가
아니라 설명서다. 그래서 각 장면의 **구조**만 블루프린트에서 가져오고,
카메라 워크·글리치·슬램 같은 과시적 동작은 쓰지 않는다. 요소는 조용히
도착하고 머문다. 제품이 "겁주지 않는다"는 원칙을 지키는데 영상이 겁을 주면
그 자체로 거짓말이 된다.

## 구조

장면 16개, 각각 별도 하위 컴포지션. 전환은 기본 `crossfade` 0.4s,
장(chapter)이 바뀌는 자리만 `cut`으로 끊어 챕터 경계를 느끼게 한다.

4장(할 수 있는 일/없는 일)은 한 장면에 88초가 필요해 둘로 나눴다. 자막만
계속 바뀌는 정지 화면이 1분 반 이어지면 보는 사람이 놓친다. 두 장면은
**같은 배치**를 쓴다 — 나란히 읽혀야 대비가 생긴다.

7장(아키텍처)은 다섯 장이다. 아키텍처 문서의 다이어그램 §1~§5 를 순서대로
설명하기 때문이다. §1(시스템 구성)은 5개 층이 한 화면에 다 들어가면 글씨가
영상에서 읽히지 않아 위(원천·모니터)와 아래(저장·판단·전달)로 나눴다.
§6(설치와 강화의 경로)은 아키텍처가 아니라 설치 이야기라 5장으로 옮겼다.

**다이어그램을 그대로 옮기지 않는다.** 원본은 문서용이라 보조 설명이 10~12px 다.
영상에서는 읽히지 않으므로 상자당 제목과 한 줄까지만 남기고 나머지는 자막이
말한다. 구조·상자·용어는 원본 그대로 둔다.

---

## Frame 1 — 표지

- status: built
- src: compositions/frames/01-title.html
- duration: 17s
- transition_in: cut
- scene: 제목과 한 줄 소개. 초록 세로 획이 왼쪽에 서고 제목이 올라온다.
- blueprint: titlecard-reveal
- rules: kinetic-beat-slam(절제 사용 — 슬램 없이 도착만), gradient-text-sweep(미사용)
- voiceover: |
    내 컴퓨터 지킴이.
    보안을 모르는 사람이 자기 컴퓨터를 스스로 지킬 수 있게 만든 도구입니다.

    무엇이고, 왜 필요하고, 어떻게 쓰는지 순서대로 보여드리겠습니다.

왼쪽에 초록 세로 획(22px)이 먼저 서고, 그 옆으로 작은 눈썹 글씨
"개인 컴퓨터를 혼자 관리하는 분을 위한"이 뜬 뒤 큰 제목 두 줄이 올라온다.
움직임은 이 셋뿐이다. 배경은 초보자 팔레트.

---

## Frame 2 — 왜 필요한가

- status: built
- src: compositions/frames/02-why.html
- duration: 49s
- transition_in: crossfade
- scene: auth.log 원문 한 줄이 화면을 채웠다가, 사람의 말로 바뀐다.
- blueprint: kinetic-type-beats
- rules: discrete-text-sequence, gradient-text-sweep(강조 단어에만)
- voiceover: |
    리눅스 컴퓨터는 이미 많은 것을 기록하고 있습니다. 누가 로그인을
    시도했는지, 어떤 프로그램이 설치됐는지, 어떤 접속이 막혔는지.

    문제는 그 기록을 아무도 읽지 않는다는 것입니다. 읽으려 해도 어렵습니다.

    이 한 줄이 위험한 일인지, 흔한 일인지, 지금 무언가 해야 하는 일인지
    이것만 보고는 알 수 없습니다.

    그래서 대부분은 아무것도 하지 않습니다. 무슨 일이 생겨도 몇 달 뒤에 압니다.

    지킴이는 이 사이를 메웁니다. 이미 남고 있는 기록을 대신 읽고,
    사람의 말로 바꾸고, 지금 해야 할 일이 있으면 그것만 알려줍니다.

세 단계로 진행한다.
(1) 검은 바탕에 고정폭 로그 세 줄이 흘러간다 — `Failed password for invalid
user admin from 203.0.113.9 port 41022 ssh2` 같은 지어낸 줄. 실제 서버 값이
아니어야 한다.
(2) 가운데 한 줄만 남고 나머지가 흐려진다.
(3) 그 줄이 초보자 팔레트의 카드로 바뀐다 — "처음 보는 곳에서 계정 이름을
찍어보고 있어요. 모두 막혔어요." 검정에서 흰 카드로 넘어가는 이 한 번의
전환이 이 장의 전부다.

---

## Frame 3 — 화면 구성 개요

- status: built
- src: compositions/frames/03-map.html
- duration: 47s
- transition_in: cut
- scene: 두 모드와 초보자 모드의 네 페이지를 지도로 그린다.
- blueprint: grid-card-assemble
- rules: anchored-layout-expand, stat-bars-and-fills(미사용)
- voiceover: |
    화면은 두 가지입니다. 쉬운 화면과 전문가 화면. 오른쪽 위에서 언제든
    바꿀 수 있고, 고른 화면은 기억됩니다.

    쉬운 화면은 '지금 무엇을 해야 하는가'만 말합니다. 상태 한 줄, 할 일이
    있으면 카드, 그리고 안심해도 되는 숫자 셋.

    여기서 세 곳으로 들어갑니다. 할 일 상세, 무슨 일이 있었는지 기록,
    그리고 지킴이가 본 것 자세히 보기. 셋 다 같은 쉬운 화면의 다른 페이지입니다.

    전문가 화면은 다릅니다. 예전부터 쓰던 대시보드 그대로, 열한 개 패널을
    한 화면에 펼칩니다. 익숙한 사람에게는 이쪽이 빠릅니다.

왼쪽 3분의 2에 지도, 오른쪽에 모드 전환기 확대 그림.
지도는 두 덩이다. 위 덩이(초보자 모드, 흰 카드 넷: 홈·할 일 상세·기록·자세히
보기)가 먼저 조립되고, 아래 덩이(전문가 모드, 검은 카드 하나)가 나중에 붙는다.
둘 사이를 잇는 것은 화살표가 아니라 모드 전환기 그림이다 — 전환이 두 종류라는
것이 이 장의 숨은 요점이다.

---

## Frame 4 — 시나리오 하나: 아무 일 없는 날

- status: built
- src: compositions/frames/04-scenario-calm.html
- duration: 34s
- transition_in: cut
- scene: 쉬운 화면 홈. '이상 없음'과 숫자 셋.
- blueprint: device-surface-showcase
- rules: anchored-layout-expand, counting-dynamic-scale(숫자 셋에만, 절제)
- voiceover: |
    첫 번째 시나리오. 아무 일도 없는 날입니다.

    화면에서 가장 큰 글씨가 '이상 없음'이면, 정말로 아무것도 하지 않으셔도
    됩니다. 창을 닫으셔도 됩니다.

    아래 숫자 셋은 안심하시라고 두는 것입니다. 밖에서 들어올 수 있는 문,
    밀린 보안 업데이트, 막힌 접속 시도.

    손볼 일이 없으면 할 일 카드는 아예 뜨지 않습니다. 빈 목록도 띄우지 않습니다.

화면 목업 하나를 크게(1280px 폭) 가운데 놓는다. 체크 원 → '이상 없음' →
숫자 카드 셋(문 1개 / 없음 / 23번) 순으로 도착. 숫자는 0에서 세어 올라가되
0.6초 안에 끝난다 — 과시하지 않는다.
마지막에 '할 일 카드가 들어갈 자리'를 점선으로 잠깐 비추고 지운다.
비어 있음이 기본값이라는 것을 눈으로 보여주는 유일한 방법이다.

---

## Frame 5 — 시나리오 둘: 손볼 일이 생겼다

- status: built
- src: compositions/frames/05-scenario-fix.html
- duration: 59s
- transition_in: cut
- scene: 할 일 카드 → 미리보기 표 → 결과. 커서가 끌고 간다.
- blueprint: cursor-ui-demo
- rules: anchored-layout-expand, discrete-text-sequence, stat-bars-and-fills(미사용)
- voiceover: |
    두 번째. 지킴이가 손볼 일을 찾았습니다.

    '다른 사람이 고칠 수 있는 파일 3개.' 이 컴퓨터를 쓰는 다른 계정이 내 설정
    파일을 바꿀 수 있는 상태라는 뜻입니다. 바뀌면 다음에 로그인할 때
    그 내용이 내 이름으로 실행됩니다.

    누르면 바로 고치지 않습니다. 먼저 무엇이 어떻게 바뀌는지 표로 보여드립니다.
    왼쪽이 지금, 오른쪽이 잠근 뒤입니다.

    잠그기를 누르면 결과 화면이 뜹니다. 여기서 쓰는 말은 미리보기에서 쓴 말과
    똑같습니다. 말이 달라지면 무슨 일이 일어났는지 대조할 수 없기 때문입니다.

    지킴이는 자물쇠를 잠그기만 하고 열지는 않습니다. 되돌리는 것은 권한을
    넓히는 일이라 사람이 직접 하도록 남겨뒀고, 방법은 기록 화면에 있습니다.

화면 세 개가 같은 자리에서 갈아 끼워진다(카메라를 움직이지 않는다).
① 홈 + 노란 테두리 할 일 카드 → 커서가 카드를 누른다
② 미리보기 화면: 전·후 표 3줄. '누구나 고칠 수 있음' → '나만 고칠 수 있음'
③ 결과 화면: 자물쇠 아이콘 + 같은 표, 다만 왼쪽이 '잠그기 전'으로 바뀌어 있다
②와 ③의 표 문구가 **글자까지 같다**는 것이 보이도록, 전환 때 표 행은 제자리에
두고 헤더와 아이콘만 바꾼다.

---

## Frame 6 — 시나리오 셋: 지킴이가 대신 못 하는 일

- status: built
- src: compositions/frames/06-scenario-judge.html
- duration: 61s
- transition_in: cut
- scene: 처음 보는 곳에서 로그인 성공. 사실만 보여주고 묻는다.
- blueprint: cursor-ui-demo
- rules: anchored-layout-expand, discrete-text-sequence
- voiceover: |
    세 번째. 지킴이가 대신 결정할 수 없는 일입니다.

    처음 보는 곳에서 로그인이 성공했습니다. 이게 위험한 일인지 아닌지는,
    그때 본인이 접속했는지 아닌지에 달려 있습니다. 그건 사용자만 압니다.

    그래서 고치지 않고 사실만 보여드리고 물어봅니다. 언제, 어디서,
    어떻게 들어왔는지.

    답에 따라 다음이 달라집니다. '네, 제가 했어요'는 확인 처리하고 닫습니다.
    '아니요'는 긴급으로 올리고 순서대로 안내합니다. '모르겠어요'는 열어둔 채
    재촉하지 않습니다.

    기억이 안 나시면 '지킴이가 본 것'을 펼쳐 근거를 보시고, 그래도 모르겠으면
    설명을 통째로 복사해 인공지능에게 물어보실 수 있습니다. 복사할 때 컴퓨터
    이름과 계정, 인터넷 주소는 자동으로 가려집니다.

① 상세 화면: 빨간 눈썹 '지금 확인하세요' + 사실 표(언제/어디서/어떻게) +
질문 한 줄 + 선택지 셋. 선택지는 순서대로 도착한다.
② 선택지 셋에서 각각 뻗어나가는 결과를 오른쪽에 짧게 적는다(세 갈래).
③ 아래 '지킴이가 본 것 1건 — 열어보기'가 펼쳐지며 원문 근거가 나오고,
그 아래 '복사하기' 버튼. 복사된 글에서 `<이 컴퓨터>` `<인터넷주소 1>`처럼
가려진 자리가 초록으로 한 번 반짝인다.

---

## Frame 7 — 할 수 있는 일

- status: built
- src: compositions/frames/07-can.html
- duration: 50s
- transition_in: cut
- scene: 실제로 도는 감시 항목이 두 줄로 쌓인다.
- blueprint: grid-card-assemble
- rules: anchored-layout-expand
- voiceover: |
    이 도구가 할 수 있는 일과 없는 일을 분명히 하겠습니다. 먼저 할 수 있는 것.

    SSH 로그인 실패와 성공, sudo 사용, 계정 변경. 브루트포스 차단 상태.
    새로 열린 포트와, 그 포트에 실제로 밖에서 닿는지.

    너무 열려 있는 파일 권한과 그것을 좁혀주는 일. 설정 파일과 자동 실행
    지점의 변조. 리버스 셸이나 임시 디렉터리 실행 같은 프로세스 지표.

    패키지 변경과 밀린 보안 업데이트. 이 서버에 실제로 영향이 있는 취약점
    공지만 골라내기. 누가 무엇을 실행했는지. 설정 강화 점수.

왼쪽 위에 체크 표식과 '할 수 있는 것'. 아래로 항목 카드가 두 열로 쌓인다.
자막 문단 하나에 카드 세 장씩 도착한다(총 열두 장). 카드마다 오른쪽 아래에
근거가 되는 도구 이름을 작게 적는다 — auth.log, fail2ban, ss, dpkg, auditd,
Lynis, USN. 주장에 출처를 붙이는 것이 이 장의 규칙이다.
카메라는 움직이지 않는다.

---

## Frame 8 — 할 수 없는 일

- status: built
- src: compositions/frames/08-cannot.html
- duration: 52s
- transition_in: cut
- scene: 못 하는 일 여섯 가지. 앞 장과 같은 크기, 같은 밝기로.
- blueprint: grid-card-assemble
- rules: anchored-layout-expand
- voiceover: |
    할 수 없는 것.

    백신이 아닙니다. 파일을 뒤져 악성코드를 찾아내지 않습니다.
    네트워크 패킷을 뜯어보지 않습니다. 침입 차단 장비가 아닙니다.

    본인이 한 일인지 아닌지 판단할 수 없습니다. 그래서 묻습니다.
    이미 커널까지 장악당한 뒤라면, 같은 커널 위에서 도는 이 도구도 속습니다.

    직접 막지도 않습니다. 막는 것은 fail2ban과 방화벽이고, 지킴이는 그것을
    보고 기록하고 권고합니다. 공유기가 포트를 넘겨주고 있는지도 이 컴퓨터
    안에서는 알 수 없습니다.

    할 수 없는 일을 할 수 있다고 말하지 않는 것이, 이 도구의 첫 번째 원칙입니다.

앞 장과 **똑같은 배치**로 시작한다. 다만 머리 표식이 사선이고 카드가 여섯
장이다. 자리가 같아야 앞 장과 나란히 읽힌다.
**흐리게 처리하지 않는다** — 못 하는 일도 같은 크기, 같은 밝기, 같은 글꼴.
마지막 문단에서 카드가 모두 살짝 뒤로 물러나고 아래에 한 줄이 걸린다.
그 줄만 강조 초록이다.

---

## Frame 9 — 설치

- status: built
- src: compositions/frames/09-install.html
- duration: 62s
- transition_in: cut
- scene: 빌드 머신 → install.sh → harden.sh → Lynis, 그리고 되돌아오는 화살표.
- blueprint: spatial-pan-stations
- rules: anchored-layout-expand, discrete-text-sequence
- diagram: 아키텍처 문서 §6 「설치와 강화의 경로」
- voiceover: |
    설치는 두 줄입니다. 빌드 머신에서 압축본을 만들고, 대상 서버에서 설치
    스크립트를 실행합니다. 대상 서버에는 git이나 node가 필요 없습니다.

    설치 스크립트가 전용 계정과 권한, systemd 유닛과 sudoers를 놓고,
    호스트 도구 설정까지 반영합니다. fail2ban, auditd, Lynis입니다.

    그다음이 강화 스크립트입니다. 이건 항상 두 번 실행합니다. 먼저 dry-run으로
    무엇이 바뀔지 목록만 보고, 확인한 뒤에 apply로 적용합니다. 서버 역할에 따라
    켜고 끄는 플래그가 있습니다.

    적용하고 나면 Lynis가 하루 한 번 돌며 강화 작업 목록을 만듭니다.
    새 경고와 지수 하락만 알리고, 제안은 백로그로 쌓입니다.

    그 목록에서 내린 결정 — 적용할지, 수용할지, 이미 다른 방식으로 해결했는지 —
    이 다시 프로파일과 스크립트 플래그로 돌아갑니다. 한 바퀴가 닫힙니다.

네 정거장을 왼쪽에서 오른쪽으로 놓고, 자막 문단마다 하나씩 도착시킨다.
마지막 문단에서 오른쪽 끝에서 왼쪽 세 번째로 되돌아오는 화살표가 그려진다 —
이 고리가 이 장의 요점이다. 설치는 한 번이지만 강화는 반복이다.
터미널 두 줄은 첫 정거장 안에 작게 넣는다(전문가 팔레트 카드).

---

## Frame 10 — 설정

- status: built
- src: compositions/frames/10-config.html
- duration: 65s
- transition_in: cut
- scene: secdash.env 한 파일. 서버마다 채우는 세 줄만 밝아진다.
- blueprint: transcript-scroll-artifact-reveal
- rules: gradient-text-sweep(강조 줄), anchored-layout-expand
- voiceover: |
    설정은 한 파일입니다. /etc/secdash/secdash.env.

    기본값만으로도 돕니다. 서버마다 채우는 것은 셋입니다.

    첫째, Slack 웹훅. 알림 제목에 호스트 이름이 붙어서, 여러 서버가 같은
    웹훅을 써도 어느 서버 일인지 구분됩니다.

    둘째, 서버 역할 메모. '이 서버는 무엇을 하는 서버인가'를 한 줄 적어두면,
    강화 작업 목록을 인공지능에게 물어볼 때 맥락으로 함께 나갑니다.

    셋째, fail2ban이 무시할 주소 대역. 관리자 대역을 넣어 자기 자신을 차단하는
    일을 막습니다. 고친 뒤 apply-host-config.sh 를 다시 실행하면 반영됩니다.

    호스트 로그는 어떤 경우에도 밖으로 나가지 않습니다. 인공지능 키를 넣어도
    번역되는 것은 공개된 보안 공지 제목뿐입니다.

전문가 팔레트에서 시작해 파일 내용이 한 화면에 담긴다(스크롤하지 않는다 —
파일이 짧다는 것 자체가 메시지다). 기본값 줄은 흐리게 두고, 세 줄만 차례로
밝아진다. 마지막 문장에서 화면 아래에 잠금 아이콘과 함께 "호스트 로그는
나가지 않습니다" 한 줄이 걸린다.

---

## Frame 11 — 아키텍처 ① 어디서 들어오나

- status: built
- src: compositions/frames/11-arch-sources.html
- duration: 66s
- transition_in: cut
- scene: 진실 원천 여섯 + 모니터 여섯 묶음 + 이벤트 버스. 위에서 아래로.
- blueprint: grid-card-assemble
- rules: anchored-layout-expand
- diagram: 아키텍처 문서 §1 「시스템 구성과 데이터 흐름」 1~2층
- voiceover: |
    아키텍처를 보겠습니다. 데이터는 위에서 아래로만 흐릅니다.

    맨 위가 진실의 원천입니다. rsyslog가 남기는 auth.log와 auditd의 audit.log,
    dpkg와 apt, /proc, /etc와 cron과 systemd, 그리고 fail2ban.
    전부 root가 소유한 데몬과 로그입니다. 지킴이가 만든 것은 하나도 없습니다.

    오른쪽 끝 점선 상자만 인터넷 밖입니다. 우분투 보안 공지와 뉴스를
    한 시간에 한 번 읽어옵니다. 읽기만 하고 아무것도 보내지 않습니다.

    그 아래가 모니터 열한 개입니다. 각자 자기 원천을 읽습니다. 로그는 따라 읽고,
    프로세스는 15초에서 30초마다 표본을 뜨고, 파일 해시는 60초마다 비교합니다.

    모니터가 하는 일은 두 가지뿐입니다. 일어난 일은 이벤트로 남기고,
    그것을 묶어 내린 판단은 알림으로 올립니다. 이 둘을 섞지 않는 것이
    이 구조의 첫 번째 규칙입니다.

두 층 + 버스. 1층 상자 여섯(맨 오른쪽만 점선 = 인터넷 밖), 화살표, 2층 모니터
상자 여섯(열한 모니터를 역할별로 묶은 것), 그 아래 초록 버스 선 한 줄.
2층 전체를 감싸는 점선 경계에 `secdash.service · User=secdash · 127.0.0.1:8000`.
자막 문단마다 한 층씩 도착한다. 마지막 문단에서 버스 선 위로
'사실 = 이벤트' / '판단 = 알림' 두 갈래가 갈라진다.

**글씨 크기는 문서가 아니라 영상 기준이다.** 원본 다이어그램의 10~12px 보조
설명은 그대로 옮기지 않는다. 상자당 제목 24px + 한 줄 설명 20px 까지만 두고,
나머지는 자막이 말한다.

---

## Frame 12 — 아키텍처 ② 어디에 쌓이고 어디로 나가나

- status: built
- src: compositions/frames/12-arch-core.html
- duration: 72s
- transition_in: crossfade
- scene: SQLite · 알림 엔진 · DefconWatcher · 알림 큐 · API · 브라우저. 인터넷 경계는 점선.
- blueprint: grid-card-assemble
- rules: anchored-layout-expand
- diagram: 아키텍처 문서 §1 「시스템 구성과 데이터 흐름」 3~5층
- voiceover: |
    사실은 SQLite에 쌓입니다. 원시 이벤트는 30일, 알림은 90일 보관합니다.
    파일 기준선, 처음 보는 주소, 차단 목록도 여기 있습니다.

    판단은 알림 엔진이 맡습니다. 같은 지문이 다시 오면 새 알림을 만들지 않고
    횟수를 올립니다. 심각도가 오르면 확인했던 알림도 다시 열립니다.

    위험 단계를 정하는 것은 따로 도는 감시 스레드입니다. 10초마다,
    아직 확인되지 않은 알림만 셉니다.

    Slack으로 나가는 것은 큐를 거칩니다. 워커 하나가 분당 열 건까지만 보내고
    나머지는 묶습니다. 알림이 몰려도 모니터가 멈추지 않습니다.

    API는 127.0.0.1에만 붙습니다. 모든 요청에 토큰 헤더가 필요하고,
    원격에서 볼 때는 SSH 터널을 씁니다.

    호스트 밖으로 나가는 것은 셋뿐입니다. 공개 피드 읽기, 뉴스 제목 번역,
    그리고 Slack 웹훅. auth.log 줄과 sudo 명령과 패키지 목록은 나가지 않습니다.

앞 장의 버스 선이 화면 맨 위에 남아 있는 채로 시작한다 — 두 장이 한 그림이라는
것이 보여야 한다. 왼쪽에 SQLite 표(테이블 일곱 줄), 가운데 위아래로 알림 엔진과
DefconWatcher, 오른쪽에 알림 큐. 그 아래 API 띠, 맨 아래 브라우저.
인터넷을 넘는 상자 셋(번역·Slack·피드)만 점선 주황이다.
마지막 문단에서 점선 상자 셋만 남고 나머지가 흐려진다 — '나가는 것은 이것뿐'.

---

## Frame 13 — 아키텍처 ③ 사건 하나가 지나가는 길

- status: built
- src: compositions/frames/13-arch-path.html
- duration: 68s
- transition_in: cut
- scene: 브루트포스 두 줄. 위는 사실 → 경고 → 차단, 아래는 성공 → 긴급 → Slack.
- blueprint: spatial-pan-stations
- rules: anchored-layout-expand, discrete-text-sequence
- diagram: 아키텍처 문서 §2 「사건 하나가 지나가는 길: SSH 브루트포스」
- voiceover: |
    사건 하나가 지나가는 길을 따라가 보겠습니다. SSH 브루트포스입니다.

    auth.log에 실패 줄이 찍힙니다. 파서가 계정과 주소와 방식을 뽑습니다.
    이름이 비슷한 게 아니라 정확히 일치할 때만 뽑습니다.

    여기까지는 사실입니다. 이벤트로 남고 심각도는 '정보'입니다.
    위험 단계에 영향을 주지 않습니다.

    30분 안에 같은 주소에서 다섯 번을 넘기면, 그때 처음 판단이 생깁니다.
    경고 알림이 뜨고 fail2ban에 차단을 요청합니다. fail2ban을 쓸 수 없으면
    차단하지 않고 '차단 권고'로 남기고 명령을 보여줍니다.

    그런데 같은 주소에서 로그인이 성공하면 이야기가 달라집니다. 실패 이력이 있는
    주소, 처음 보는 주소, 게다가 root. 이건 곧바로 긴급입니다.

    긴급 알림이 열려 있으면 감시 스레드가 단계를 올리고 Slack이 나갑니다.

    시뮬레이션으로 넣은 사건은 같은 길을 가되 표시가 붙어서,
    위험 단계에도 Slack에도 fail2ban에도 닿지 않습니다.

가로 다섯 칸 × 두 줄. 윗줄은 회색·노랑(사실 → 경고), 아랫줄은 빨강(긴급).
두 줄을 잇는 세로 점선에 '같은 IP의 실패 창 유지'.
윗줄이 먼저 다 도착한 뒤 아랫줄이 도착한다 — 시간 순서가 그대로 보여야 한다.
맨 아래 한 줄로 시뮬레이션 단서.

---

## Frame 14 — 아키텍처 ④ 알림의 생애주기와 위험 단계

- status: built
- src: compositions/frames/14-arch-alert.html
- duration: 58s
- transition_in: cut
- scene: 열림 → 확인함 → 해결됨. 되돌아가는 화살표 둘.
- blueprint: comparison-split
- rules: anchored-layout-expand
- diagram: 아키텍처 문서 §3 「알림의 생애주기와 DEFCON」
- voiceover: |
    알림에는 세 가지 상태가 있습니다.

    열림. 아직 아무도 보지 않은 상태이고, 위험 단계에 반영되는 것은 이것뿐입니다.

    확인함. 사람이 '봤다'고 표시한 상태입니다. 대응은 진행 중이지만
    단계 계산에서는 빠집니다.

    해결됨. 끝난 것입니다. 90일 뒤에 지워집니다.

    되돌아가는 길도 있습니다. 확인한 뒤에 심각도가 더 오르면 다시 열립니다.
    반대로 원인이 사라지면 — 보안 업데이트를 설치했거나 사용량이 정상으로
    돌아오면 — 시스템이 스스로 해결로 넘깁니다.

    점검 모드를 켜면 설정이나 패키지 변경 같은 계획된 일은 처음부터 확인함으로
    태어납니다. 근거는 남지만 Slack은 가지 않습니다.

    다만 브루트포스나 리버스 셸 같은 침입 신호는 점검 모드와 무관하게
    언제나 열림입니다.

가운데에 알약 세 개를 가로로. 위로 '같은 지문 재발 → 횟수 +1', 아래로
'자동 해결' 큰 곡선, 사이에 '심각도 상승 → 재오픈' 역방향 화살표.
맨 아래 한 줄: 'DEFCON 1 = 열린 긴급 · DEFCON 3 = 열린 경고 · SAFE = 열린 것 없음'.
이 장은 시나리오 셋(사용자 답변으로 알림이 닫히는 화면)의 뒷면이므로,
'확인함' 알약만 강조 초록이다.

---

## Frame 15 — 아키텍처 ⑤ 권한의 경계, 그리고 왜 이 구조인가

- status: built
- src: compositions/frames/15-arch-trust.html
- duration: 62s
- transition_in: cut
- scene: 왼쪽에 경계 표, 오른쪽에 설계 원칙 네 줄.
- blueprint: comparison-split
- rules: anchored-layout-expand
- diagram: 아키텍처 문서 §5 「신뢰 경계와 권한」 + §4 「왜 이 구조인가」
- voiceover: |
    마지막으로 권한입니다. 지킴이는 root로 돌지 않습니다.

    전용 계정이 adm 그룹으로 auth.log를 읽고, 능력 세 개만 더 받습니다.
    보호된 파일을 읽는 것, 다른 사용자 프로세스의 실행 파일과 소켓을 확인하는 것,
    네트워크 정보를 읽는 것. 쓰기 권한은 없습니다.

    fail2ban에는 sudo로 일곱 개 명령만 허용합니다.
    그 밖의 root 명령은 전부 막혀 있습니다.

    왜 이렇게 만들었는지 네 줄로 정리하겠습니다.

    진실의 원천은 하나씩 둡니다. 대시보드는 해석만 하고 두 번째 사실을
    만들지 않습니다. 조용히 실패하지 않습니다 — 읽지 못하는 파일은
    '정상'이 아니라 '제한'입니다.

    사실과 판단을 나눕니다. 사람의 시간을 요구하는 것은 알림뿐입니다.
    그리고 호스트 로그는 안에 머뭅니다. 한국어 문장은 서버 안에서
    템플릿으로 만듭니다.

왼쪽 표 여섯 줄(경계 / 넘는 것 / 허용 방법). '막는 것' 열은 영상에서 빼고
자막이 말한다 — 네 열은 화면에서 읽히지 않는다.
오른쪽 원칙 네 줄은 자막 마지막 두 문단에 맞춰 둘씩 도착한다.

---

## Frame 16 — 맺음말

- status: built
- src: compositions/frames/16-closing.html
- duration: 29s
- transition_in: crossfade
- scene: 지키는 원칙 셋. 초록 바탕에 흰 글씨.
- blueprint: titlecard-reveal
- rules: kinetic-beat-slam(도착만)
- voiceover: |
    정리하겠습니다. 이 도구는 세 가지를 지킵니다.

    첫째, 할 일이 없으면 아무 말도 하지 않습니다.
    둘째, 고치기 전에 무엇이 바뀌는지 먼저 보여줍니다.
    셋째, 모르는 것은 모른다고 말하고, 사용자만 아는 것은 사용자에게 묻습니다.

    보안을 몰라도 됩니다. 화면이 무엇을 해야 할지 알려줍니다.

바탕은 강조 초록(`#0e7c6b`) 전면. 번호 원 셋이 차례로 도착하고 각 줄이 붙는다.
마지막 문장에서 셋이 흐려지고 가운데에 한 줄만 남는다.
